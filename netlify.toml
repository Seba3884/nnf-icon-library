# Netlify configuration for the NNF Icon Library site.
# The site is a self-contained static page in site/ — no build step required.
[build]
  publish = "site"

[[headers]]
  for = "/*.svg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
